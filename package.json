{
  "name": "tokenexchange",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "mkdir dist && rm -f dist/TokenExchange.zip ; zip dist/TokenExchange.zip -r *.js *.json node_modules/"
  },
  "author": "zeekhoo-okta",
  "license": "ISC",
  "dependencies": {
    "@okta/jwt-verifier": "^1.0.0",
    "query-string": "^6.9.0"
  }
}
